$share(print)

`extern WriteFile`
`extern GetStdHandle`

; print($rdx, $r8d, ARGS, var, line_length)

func print()
    `sub  rsp, 40`
    `mov  rcx, -11`
    `call GetStdHandle`
    `mov  rcx, rax`
    `xor  r9, r9`
    `mov  qword [rsp + 32], 0`
    `call WriteFile`
    `add  rsp, 40`
end
