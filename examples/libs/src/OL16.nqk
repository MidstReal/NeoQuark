$share(printch)
$share(readch)
$share(cls)
$share(exit)

func printch()
    `mov ah, 0x0e`
    `int 0x10`
end

func cls()
    `mov ax, 0x0003`
    `int 0x10`
end

func readch()
    `mov ah, 0x00`
    `int 0x16`
end

func exit()
    `jmp $`
end