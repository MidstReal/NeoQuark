$share(_start)
$include("libs/win.asm")

`section .text`

func _start()
    ; print($rsi, $rdx, ARGS, var, line_length)

    print($rsi, $rdx, ARGS, msg, 23)
    exit()

`section .data`
`msg db "Hello, Linux!", 10`
