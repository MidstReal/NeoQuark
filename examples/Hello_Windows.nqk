; compile:
; nquark.exe input.nqk -o output.asm -64
; nasm.exe -f win64 output.asm -o prog.obj
; golink.exe prog.obj kernel32.dll /console /entry:_start

; code for version 26-7

$share(_start)

$include("libs/win64.asm")

$section(.text)
func _start
	print&(msg, 25)
end

$section(.data)
char msg = "Hello, Windows!",10
