$share(_start)
$include("libs/win.asm")

`section .text`
func _start()
    ; print($rdx, $r8d, ARGS, var, line_length)

    print($rdx, $r8d, ARGS, msg, 25)
end
`section .data`
`msg db "Hello, Windows!", 10`
