#ifndef GLOBAL_H
#define GLOBAL_H

#include <iostream>
#include <fstream>
#include <string>
#include <vector>

using namespace std;

extern ifstream in;
extern ofstream out;
extern string line;
extern int linenum;
extern bool mode64;
extern bool mode32;
extern bool mode16;
extern bool mode8;
extern bool dbg;

#endif